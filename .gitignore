# Keep Obsidian vault and generated docs local
docs/

# Brainstorming session mockups
.superpowers/

# Node / build artifacts
node_modules/
out/
dist/
*.tsbuildinfo

# npm is the package manager; keep pnpm artifacts out
pnpm-lock.yaml
pnpm-workspace.yaml

# tourney state — local only
data/*.json

# bundled OAuth credentials — never commit
src/main/auth/oauth-config.ts
!src/main/auth/oauth-config.example.ts
